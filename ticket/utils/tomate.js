const {SlashCommandBuilder}=require('discord.js');const S=require('../utils/systems/staffPromotion');module.exports={data:new SlashCommandBuilder().setName('resetar_pontos').setDescription('Reseta pontos de um staff').addUserOption(o=>o.setName('staff').setDescription('Staff').setRequired(true)),async execute(c,i){if(!i.member.permissions.has('Administrator')&&!i.member.roles.cache.has(S.SECRETARIA))return i.reply({content:'❌ você não tem permissão.',ephemeral:true});const m=i.options.getMember('staff');await S.set(m.id,0);await i.reply(`♻️ pontos de ${m} resetados.`);}};
