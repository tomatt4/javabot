const { SlashCommandBuilder, PermissionFlagsBits, EmbedBuilder } = require('discord.js');
const { canAct, parseDuration } = require('../utils/systems/common');
module.exports={data:new SlashCommandBuilder().setName('mute').setDescription('Muta um membro').addUserOption(o=>o.setName('membro').setDescription('Membro alvo').setRequired(true)).addStringOption(o=>o.setName('duracao').setDescription('Ex.: 10min, 1h, 30sec').setRequired(true)).addStringOption(o=>o.setName('motivo').setDescription('Motivo').setRequired(false)).setDefaultMemberPermissions(PermissionFlagsBits.ModerateMembers),async execute(client,interaction){const member=interaction.options.getMember('membro');const reason=interaction.options.getString('motivo')||'sem motivo informado';const [ok,msg]=canAct(interaction.member,member,interaction.guild);if(!ok)return interaction.reply({content:msg,ephemeral:true});try{const seconds=parseDuration(interaction.options.getString('duracao'));if(!seconds)return interaction.reply({content:'use uma duração válida, por exemplo: 10min, 1h, 30sec.',ephemeral:true});await member.timeout(seconds*1000,reason);await interaction.reply(member+' foi mutado por '+interaction.options.getString('duracao')+', motivo: '+reason);}catch(e){return interaction.reply({content:'não foi possível executar a ação: '+e.message,ephemeral:true});}}};
