const { SlashCommandBuilder, PermissionFlagsBits, EmbedBuilder } = require('discord.js');
const { canAct, parseDuration } = require('../utils/systems/common');
module.exports={data:new SlashCommandBuilder().setName('kick').setDescription('Expulsa um membro').addUserOption(o=>o.setName('membro').setDescription('Membro alvo').setRequired(true)).addStringOption(o=>o.setName('motivo').setDescription('Motivo').setRequired(false)).setDefaultMemberPermissions(PermissionFlagsBits.KickMembers),async execute(client,interaction){const member=interaction.options.getMember('membro');const reason=interaction.options.getString('motivo')||'sem motivo informado';const [ok,msg]=canAct(interaction.member,member,interaction.guild);if(!ok)return interaction.reply({content:msg,ephemeral:true});try{await member.kick(reason);await interaction.reply(member+' foi expulso, motivo: '+reason);}catch(e){return interaction.reply({content:'não foi possível executar a ação: '+e.message,ephemeral:true});}}};
