const { SlashCommandBuilder, PermissionFlagsBits, EmbedBuilder } = require('discord.js');
const { canAct, parseDuration } = require('../utils/systems/common');
module.exports={data:new SlashCommandBuilder().setName('warn').setDescription('Avisa um membro').addUserOption(o=>o.setName('membro').setDescription('Membro alvo').setRequired(true)).addStringOption(o=>o.setName('motivo').setDescription('Motivo').setRequired(false)).setDefaultMemberPermissions(PermissionFlagsBits.ManageRoles),async execute(client,interaction){const member=interaction.options.getMember('membro');const reason=interaction.options.getString('motivo')||'sem motivo informado';const [ok,msg]=canAct(interaction.member,member,interaction.guild);if(!ok)return interaction.reply({content:msg,ephemeral:true});try{const embed=new EmbedBuilder().setTitle('⚠️ Aviso').setDescription(member+' foi avisado').addFields({name:'Motivo',value:reason}).setColor(0xF1C40F).setFooter({text:'Aviso por '+interaction.user.tag,iconURL:interaction.user.displayAvatarURL()});await interaction.reply({embeds:[embed]});await member.send('Você foi avisado em '+interaction.guild.name+'. Motivo: '+reason).catch(()=>{});}catch(e){return interaction.reply({content:'não foi possível executar a ação: '+e.message,ephemeral:true});}}};
